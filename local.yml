- hosts: localhost
  become: true
  pre_tasks:
    - name: add key
      apt-key:  url =https://dl-ssl.google.com/linux/linux_signing_key.pub state=present
      
    - name: add repository for chrome
      apt_repository: url= http://dl.google.com/linux/chrome/deb/ stable main filename=google
      


  tasks:
    - include: tasks/packages.yml